[package]
name = "sandbox_core"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No external dependencies - pure std lib for maximum compatibility

[profile.release]
opt-level = 3
lto = true
